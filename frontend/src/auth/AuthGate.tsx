import { useEffect,useState,type FormEvent,type ReactNode } from 'react'
import type { Session,SupabaseClient } from '@supabase/supabase-js'
import { getSupabase } from './supabase'
import './AuthGate.css'
export function AuthGate({children}:{children:ReactNode}){const[supabase,setSupabase]=useState<SupabaseClient|null>(null),[session,setSession]=useState<Session|null>(null),[loading,setLoading]=useState(true),[configured,setConfigured]=useState(true),[email,setEmail]=useState(''),[sent,setSent]=useState(false),[error,setError]=useState<string|null>(null);useEffect(()=>{let unsubscribe=()=>{};void getSupabase().then(async(client)=>{if(!client){setConfigured(false);setLoading(false);return}setSupabase(client);const{data}=await client.auth.getSession();setSession(data.session);setLoading(false);const listener=client.auth.onAuthStateChange((_event,next)=>setSession(next));unsubscribe=()=>listener.data.subscription.unsubscribe()});return()=>unsubscribe()},[]);async function google(){if(!supabase)return;setError(null);const{error:authError}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:window.location.origin}});if(authError)setError(authError.message)}async function magicLink(event:FormEvent){event.preventDefault();if(!supabase)return;setError(null);const{error:authError}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:window.location.origin}});if(authError)setError(authError.message);else setSent(true)}if(loading)return <div className="auth-loading">Carregando GiControl…</div>;if(!configured)return <div className="auth-page"><section className="auth-card"><img src="/gicontrol-logo.png" alt="GiControl"/><h1>Configure a autenticação</h1><p>O servidor ainda não possui SUPABASE_URL e SUPABASE_ANON_KEY.</p></section></div>;if(!session)return <div className="auth-page"><section className="auth-card"><img src="/gicontrol-logo.png" alt="GiControl"/><p className="eyebrow">BEM-VINDA À GICONTROL</p><h1>Suas finanças, no seu controle</h1><p>Entre para acessar seus dados financeiros com segurança.</p><button className="google-button" type="button" onClick={()=>void google()}><b>G</b> Continuar com Google</button><div className="auth-divider"><span>ou</span></div><form onSubmit={(event)=>void magicLink(event)}><label>E-mail<input required type="email" value={email} onChange={(event)=>setEmail(event.target.value)} placeholder="voce@gmail.com"/></label><button className="primary-button" type="submit">Receber link de acesso</button></form>{sent&&<p className="auth-success">Link enviado. Confira sua caixa de entrada.</p>}{error&&<p className="auth-error">{error}</p>}</section></div>;return <>{children}</>}
